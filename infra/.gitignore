node_modules
dist
cdk.out
# cdk.context.json IS committed — see AWS docs on CDK context. It caches lookups
# (default VPC etc.) so every dev + CI synth resolves to the same values without
# hitting AWS. Delete + re-resolve if you switch accounts.
*.js
!jest.config.js
*.d.ts
.cdk.staging
